// This config is only required for antd and not related to project. 'theme' folder can be deleted if not using antd

const theme = {
  token: {
    fontSize: 16,
    colorPrimary: "#52c41a",
  },
};

export default theme;
